apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: media-volume
  name: media-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
